# Example client env for Vite (build-time)
# Set VITE_API_URL to the backend public URL (include https://)
# This MUST be set in Render static site environment (so Vite builds with correct URL)

VITE_API_URL=https://pyramidsmart-server.onrender.com
