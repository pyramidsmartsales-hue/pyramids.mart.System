// placeholder: define Joi / Zod schemas here if you plan to share between client/server
export const ClientSchema = {
  name: { type: "string", required: true },
  phone: { type: "string", required: true }
};
